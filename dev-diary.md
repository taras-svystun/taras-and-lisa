# Dev Diary — taras-and-lisa

Persistent context log for this repo. Read this first before touching anything. Kept intentionally short — put decisions, gotchas, and incidents here; let the code itself document its own structure.

## Project overview

Personal/couple brand site for Taras & Lisa (coaching, mountain guiding, snowboarding): a static Astro site plus a Telegram bot (Cloudflare Worker) that lets the owner edit site content by chatting with it. Bot calls Claude → tool-calling agent reads/writes the site's JSON content files → Zod-validated → committed via the GitHub Contents API → triggers a redeploy. Fully wired and live-tested over real Telegram, including the `/undo` safety-net layer (Stage 3, complete). Structured logging (Workers Logs) and LLM tracing (Langfuse) now cover the whole lifecycle (Stage 4, complete as of 2026-07-23).

## Repo structure

```
taras-and-lisa/
├── site/     Astro site
└── bot/      Telegram bot (Cloudflare Worker)
```

Separate npm projects (own `package-lock.json` each), no shared root package.json/workspaces — run `npm`/`astro`/`wrangler` from inside the relevant subfolder. Repo root has only `README.md`, `.gitignore`, `dev-diary.md`.

## Site (Astro)

- Astro 7.1.0, Node `>=22.12.0`, Tailwind v4. Content Collections (`site/src/content.config.ts`) — three collections, each backed by a JSON file in `site/src/data/`, and that JSON is the *only* source of copy (zero hardcoded strings in `.astro` files, which is what makes the site bot-editable):
  - **site** (`site.json`, wrapped `{main:{...}}`): `name, heroEyebrow, heroHeadline, heroSubhead, aboutHeading, aboutBody[], aboutPhotoAlt, contactHeading, contactBody, email, instagramUrl?, telegramUrl?`
  - **services** (`services.json`, flat array): `{id, title, description}[]`
  - **portfolio** (`portfolio.json`, wrapped `{main:{...}}`): `eyebrow, heading, subhead, experienceHeading, experience[{role,company,period,bullets[]}], projectsHeading, projectsSubhead, projects[{title,description,url}], educationHeading, educationBody[] (raw HTML), ctaHeading, ctaBody, ctaEmail, githubUrl`
  - **All schemas now use `.strict()`** (fixed 2026-07-23 — matches the bot-side schemas, see Rules below).
- 5 nav pages: Home/About/Services/Portfolio/Contact. Domain: **taras-and-lisa.com**.
- **Hosting — confirmed 2026-07-23:** `taras-and-lisa` is a live Cloudflare **Worker** (`wrangler deployments list --name taras-and-lisa` shows real deployment history back to 2026-07-17; `wrangler pages project list` returns zero classic Pages projects for this account) — it has been a Worker since the project's inception, not classic Pages. However, `site/wrangler.jsonc` (an explicit local config with the `@astrojs/cloudflare` adapter, an `assets` binding, and `observability`) still only exists on the un-merged `cloudflare/workers-autoconfig` branch (one real commit from `cloudflare-workers-and-pages[bot]`, not a no-op — adds the adapter to `astro.config.mjs`, `wrangler.jsonc`, `tsconfig.json`, `.assetsignore`, and bumps `package.json`/`package-lock.json`). **Do not merge or delete that branch without the owner's explicit decision** — it's real, substantive config, not throwaway bot noise. Until it's merged, `main` has no local `wrangler.jsonc` for `site/`, so there's nowhere on `main` to add Workers Logs `observability` config for the site Worker (see Secrets/Bot section — only the bot's `wrangler.jsonc` was touched this session).

## Bot (Cloudflare Workers)

Worker `typetodeploy-bot` (`bot/wrangler.jsonc`), entry `src/index.ts`, `nodejs_compat`. Telegram via **grammY**. `observability: { enabled: true, head_sampling_rate: 1 }` — every request logged, no sampling (low-traffic personal bot).

- **`bot/src/logger.ts`** — structured logging for Cloudflare Workers Logs. Exports small named functions (`logUserMessage`, `logBotReply`, `logLlmCallStart`, `logLlmCall`, `logToolCall`, `logGithubCommit`, `logRawDeployMessage`, `logDeployEvent`, `logError`), each emitting exactly **one `console.log(plainObject)` call — never `JSON.stringify`'d, never string concatenation** (Cloudflare indexes object fields for dashboard filtering; stringified/concatenated logs are text-search only). Every line has a `message` field with one emoji by category (👤 user message, 🤖 bot reply, 🧠 LLM call, 🔧/✅/❌ tool call, 📝/✅/❌ GitHub commit, 📦/✅/❌ deploy event, ❌ error), plus `event`, `chat_id`, `timestamp`. Blank `console.log()` calls separate logically distinct steps for readability in `wrangler tail`. `logError` always logs both `error.message` and `error.stack`, never just `String(error)`. **Never log secrets** — none of the six Worker secrets, or the two Langfuse keys, ever get passed to any of these.
- **`bot/src/langfuse.ts`** — Langfuse LLM tracing via the **plain HTTP ingestion API** (`POST {LANGFUSE_HOST}/api/public/ingestion`, Basic auth from `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`), NOT the official `langfuse` npm SDK — that SDK's OpenTelemetry exporter has Node.js-specific APIs that fail on the Workers runtime (a known Langfuse GitHub issue for Workers/Hono). Builds `trace-create`/`generation-create`/`span-create` event batches (parents always ordered before children referencing them via `parentObservationId` — Langfuse silently drops observations otherwise) and sends them via `ctx.waitUntil()` so a Langfuse failure or slow response never affects the bot's reply latency. One trace per Telegram message (`name: "telegram-message"`, `sessionId: chatId`), with one generation per LLM call and one span per tool call plus one span for the GitHub commit step, all children of that turn's generation.
- **Session correlation across `fetch()`/`queue()`:** when `agent.ts` commits an edit, it appends a `Langfuse-Session: <chatId>` trailer on its own line after the commit subject. `index.ts`'s `queue()` handler extracts it (`/Langfuse-Session:\s*(\S+)/`) from `buildTriggerMetadata.commitMessage` and sends a `trace-create`+`span-create` pair using that `chatId` as `sessionId`, so one conversation's full lifecycle (message → LLM → tool calls → commit → deploy outcome) shows up under one Langfuse session even though `queue()` runs in a separate Worker invocation minutes later. Silently skipped if no trailer is found (e.g. a manual GitHub edit). **The trailer is stripped (`stripLangfuseTrailer`) before the commit message is ever shown to the user in a Telegram notification.**
- **`bot/src/bot.ts`** — allowlist middleware (`ALLOWED_USER_ID` vs `ctx.from.id`, silent reject, logged inline) gates every command via `bot.use()`. Commands: `/start` (help), `/status`, `/reset` (clears KV history), `/undo` (see below). `message:text` → "⏳ Working on it..." → `runAgent()` → reply with result + a `✅ file: diffSummary — commitUrl` line per commit; the Langfuse batch `runAgent()` returns is sent via `cfCtx.waitUntil(sendToLangfuse(...))` right before the reply. `createBot()` now takes a third `cfCtx: ExecutionContext` param (threaded from `index.ts`'s `fetch()`) specifically so this and future background sends never block the Telegram response. Own try/catch around both `runAgent()` and `undoLastBotChange()` as a safety net, now logging via `logError` (chat id + step + message/stack) instead of a bare `console.error`.
- **`bot/src/conversation-memory.ts`** — per-chat history in `CONVERSATIONS` KV. **Plain `{role, text}` turns only — never raw `tool_use`/`tool_result` blocks** (agent must always re-read files fresh, never trust a replayed read). `MAX_TURNS=8`, `TTL_SECONDS=1800`.
- **`bot/src/index.ts`** — exports `fetch`, `email`, `queue`. `fetch()` logs the **raw incoming update as its literal first line** (`logUserMessage`, before any pathname routing, `/start`/`/reset` handling, or secret-header check) — targets a suspected bug where the first message after `/start` sometimes silently disappears; a repeated `update_id` in the logs would mean Telegram is redelivering the same webhook. **Only `queue` is the active deploy-notification path**: Workers Builds → Event Subscription → `deploy-events` Queue → `queue(batch, env, ctx)` → Telegram, filtered to `branch === "main"` only (Cloudflare's own `cloudflare/workers-autoconfig` branch triggers builds too, and shouldn't page anyone). **`extractBuildEventInfo` now parses Cloudflare's confirmed, documented Workers Builds event schema** (`event.type`, `event.source.workerName`, `event.payload.status`/`.buildOutcome`, `event.payload.buildTriggerMetadata.branch`/`.commitHash`/`.commitMessage`/`.author`) instead of guessing — the raw payload is still logged unconditionally first (`logRawDeployMessage`) as a permanent safety net in case `eventSchemaVersion` changes later. `fetch`'s `POST /webhooks/pages-deploy` and the `email()` handler are **dormant leftovers, kept working but unused** (webhook needed a paid plan; email got superseded once Workers Builds' structured event data became available) — both now also log their raw payload unconditionally, in case they're ever exercised for real. `extractDeployInfo`/`extractDeployInfoFromEmail` themselves were deliberately left un-"fixed" — no official schema exists for either path, unlike Workers Builds. `fetch` also serves the real `POST /telegram-webhook` route (unrelated to deploy notifications) with an outer try/catch that sends a fallback Telegram reply on any uncaught error, so the bot is never silently unresponsive. All paths share one `sendTelegramMessage(env, chatId, text)` helper (raw fetch to Telegram's API, bypasses grammY). The deploy-failure Telegram message now explicitly says `/undo` can only revert edits made through the bot's own chat, not manual GitHub edits.
- **`bot/src/content-schemas.ts`** — Zod schemas mirroring `site/src/content.config.ts` field-for-field (**manually kept in sync — no shared code**). **Every `z.object()` uses `.strict()`** (incident-driven, see below — never remove); `site/src/content.config.ts` now has `.strict()` too as of 2026-07-23 (previously a known gap).
- **`bot/src/github.ts`** — GitHub Contents/Commits REST client. **Must use `Buffer.from(...)`, never `btoa`/`atob`** — the latter silently corrupts the Cyrillic text in this site's content. No longer logs internally — callers (`agent.ts`, `undo.ts`) log via `logGithubCommit` since they're the ones with `chatId` in scope.
- **`bot/src/undo.ts`** — `undoLastBotChange(env, chatId)` (now takes `chatId` for logging only — no behavior change). Scans the last 5 commits touching `site/src/data`, finds the most recent one whose **committer name is exactly `"TypeToDeploy Bot"`**, and reverts only that. **It does NOT revert "the latest commit on main" generically** — a manual GitHub-web-UI edit is silently skipped, not reverted (confirmed by direct code read, 2026-07-17). If no bot commit is found in the last 5, it just says so and does nothing else. Double-undo (running it twice in a row) re-applies the original change — expected, not a bug, documented in `/start`'s help text. Each reverted file now logs via `logGithubCommit`; the outer failure path logs via `logError`.
- **`bot/src/agent.ts`** — `runAgent(env, userMessage, history, chatId, updateId)` (now takes `chatId`/`updateId`, and returns a `langfuseBatch` alongside `finalText`/`commits`), hand-rolled Claude tool-calling loop (`MODEL="claude-haiku-4-5-20251001"`, `MAX_TOKENS=2048`, `MAX_ITERATIONS=6`, `TEMPERATURE=0.2` as of 2026-07-24, loop termination manually verified — no off-by-one). Two tools: `read_content_file`, `update_content_file`. **Critical invariant: `content` passed to `update_content_file` must always be the COMPLETE file** — any omitted field is silently deleted on the next commit. System prompt also forbids inventing fields not in a schema. **SCOPE CONFINEMENT rule (added 2026-07-24):** every field of `site`/`portfolio` is grouped into a named section (Home/About/Contact; Header/Experience/Projects/Education/CTA) in the prompt, with an explicit rule against touching a sibling section plus a mandatory pre-commit self-check — see History below for the incident that motivated this. `SECTION_GROUPS` in this file mirrors those same groupings for the Telegram confirmation message (`summarizeObjectDiff`) — keep both in sync if the prompt's groupings ever change. Separate TONE AND STYLE block governs phrasing only (casual, user's language, no corporate filler). Every LLM call and tool call is now logged (`logLlmCallStart`/`logLlmCall`/`logToolCall`) and mirrored into the Langfuse batch as a `generation-create`/`span-create` pair; the GitHub commit inside `update_content_file` appends the `Langfuse-Session` trailer described above.

## Secrets and environment variables

**Never write actual secret values into this file — names only.**

Cloudflare Worker secrets: `ALLOWED_USER_ID`, `BOT_TOKEN`, `WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`, `GITHUB_PAT`, `CF_WEBHOOK_SECRET`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`. All eight placeholders in `bot/.dev.vars.example`; real `bot/.dev.vars` is gitignored. `CF_WEBHOOK_SECRET` guards the now-dormant `/webhooks/pages-deploy` route (checked against `cf-webhook-auth` header) — never set by any session, still needs `wrangler secret put CF_WEBHOOK_SECRET` if that path is ever revived. `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` were set directly by the project owner via `wrangler secret put` (never seen or logged by any session) — set both as secrets even though the public key isn't strictly sensitive, for consistency.

Plain vars (`bot/wrangler.jsonc`): `GITHUB_OWNER="taras-svystun"`, `GITHUB_REPO="taras-and-lisa"`, `LANGFUSE_HOST="https://cloud.langfuse.com"` (EU region). KV binding: `CONVERSATIONS` (id `4ca6550cbce043e789571a9e7955dccd`). Queue consumer: `deploy-events`.

GitHub PAT: fine-grained, scoped to this repo only, `contents: read/write` + `metadata: read`.

## GitHub repo info

Owner **taras-svystun**, repo **taras-and-lisa**, default branch **main**, remote `git@github.com:taras-svystun/taras-and-lisa.git`.

## Rules — things NOT to do

- Never commit secret values anywhere in this repo (code, `.dev.vars`, this diary, commit messages, logs — every `logger.ts` call must pass individual scalar fields, never spread `env`; the Langfuse keys are never logged, not even truncated).
- Never assume file structure — read the actual file before editing (this repo has been restructured before).
- Content JSON files (`site/src/data/*.json`) are the source of truth — no hardcoded copy in `.astro` files.
- Don't loosen a Zod schema to make bad data pass — fix the data instead.
- `bot/src/content-schemas.ts` duplicates `site/src/content.config.ts` — changing one means updating the other in the same change.
- `bot/src/github.ts` must use `Buffer.from(...)`, never `btoa`/`atob` (Cyrillic corruption bug).
- `content` passed to `update_content_file` must always be the COMPLETE file, never a partial patch.
- Never persist raw `tool_use`/`tool_result` blocks in conversation history.
- **All Zod `z.object()` schemas in both `bot/src/content-schemas.ts` and `site/src/content.config.ts` must use `.strict()`** — silent unknown-key stripping caused a false-success production commit once (see incident below). Never remove it from either file.
- Every `logger.ts` function emits exactly one `console.log(plainObject)` — never `JSON.stringify` it into a string, never string-concatenate. Cloudflare only indexes object fields for dashboard filtering.
- Do NOT install the `langfuse` npm SDK in `bot/` — it fails on the Workers runtime. Use the plain HTTP ingestion API (`bot/src/langfuse.ts`) instead.
- Always `git fetch` and check remote-ahead-of-local before pushing — the bot and Cloudflare's platform integration can both commit to `main` outside any local session's knowledge.
- After scaffolding new top-level files/dirs, verify with `git status` that they're trackable.
- Don't merge or delete the `cloudflare/workers-autoconfig` branch without the owner's explicit sign-off — it contains real, substantive config, not bot noise.
- Never let an edit scoped to one named section (e.g. "home page") touch a sibling section in the same content file, even if it would look stylistically consistent — this was a real incident (see 2026-07-24 History entry), not a hypothetical. If `SYSTEM_PROMPT`'s section groupings in `bot/src/agent.ts` ever change, update `SECTION_GROUPS` in the same file to match — they're deliberately kept in sync so the Telegram confirmation message can't silently drift from what the prompt actually enforces.

## Open items (not yet resolved)

- Real-world event/payload shapes for the two dormant deploy-notification paths (webhook JSON, email text) were guessed defensively, never confirmed against a real delivery — `extractDeployInfo`/`extractDeployInfoFromEmail` in `bot/src/index.ts` are unchanged from that guesswork. (The active Workers Builds queue path's `extractBuildEventInfo` was fixed this session against Cloudflare's confirmed schema; all three paths now log their raw payload unconditionally as a safety net regardless.)
- `README.md` references `docs/screenshot.png` and `docs/demo.gif` — neither exists yet (the `docs/` folder itself was created this session so the paths resolve); owner is adding both separately.
- Whether the `cloudflare/workers-autoconfig` branch (real `@astrojs/cloudflare` adapter + `site/wrangler.jsonc` + `observability` config) should be merged to `main` — left for the owner to decide; not merged or deleted this session.

## History (condensed)

**2026-07-24 — Scope-confinement prompt fix:**
- **Incident:** owner asked the bot to "Add emojis to the home page text... Only to the home page." The bot added emojis to the home page fields AND the about-section fields (`aboutBody`), because `site.json`'s flat schema bundles home/about/contact fields together with no explicit section boundary in the system prompt — the model had to infer the boundary and got it wrong (confirmed by reading the actual commit diff, `9c35579`). A specification gap, not a one-off slip.
- **Fix:** `SYSTEM_PROMPT` in `bot/src/agent.ts` rewritten to explicitly group every field of `site` and `portfolio` under a named section (Home/About/Contact; Header/Experience/Projects/Education/CTA), added a SCOPE CONFINEMENT rule forbidding edits to sibling sections even when "stylistically consistent," a mandatory pre-commit self-check (list every field about to change, verify each is explicitly named or inside the one section/item the user referred to), and three worked examples (section-scoped, ambiguous-scope, single-array-item). All prior correct behavior (read-before-write, complete-file-content, ambiguity → clarifying question, fixed field list, tone/style) preserved unchanged.
- **Also:** `temperature: 0.2` added to the `messages.create` call (was previously unset, i.e. API default) — trims variance-driven scope creep on top of the prompt fix; doesn't fix systematic misunderstanding on its own.
- **Bonus:** the Telegram confirmation message for `site`/`portfolio` commits now groups changed field names by the same sections defined in the prompt (e.g. `Changed: Home (heroHeadline, heroSubhead).`) instead of a flat field list — `services` unaffected (still per-item added/removed/changed). New `SECTION_GROUPS` map in `agent.ts`, mirrors the prompt's groupings exactly so the two can't silently drift apart without a diff catching it.

**2026-07-23 — Observability + LLM tracing (Stage 4):**
- Structured logging rewritten from a generic `logEvent()` bag into named, single-purpose functions in `bot/src/logger.ts` (see Bot section above) — every call site across `bot.ts`, `agent.ts`, `github.ts`, `undo.ts`, `index.ts` migrated.
- Langfuse tracing added end-to-end (`bot/src/langfuse.ts`, plain HTTP ingestion, no SDK) with session correlation between the `fetch()` edit flow and the later `queue()` deploy event via a `Langfuse-Session` commit trailer.
- `extractBuildEventInfo` rewritten against Cloudflare's confirmed Workers Builds event schema (was previously defensive guesswork, same as the other two deploy paths still are).
- Confirmed the Workers Builds → `deploy-events` Event Subscription already existed (`workersBuilds.worker` source, `taras-and-lisa` resource, `build.failed`/`build.succeeded` — created before this session, not by it).
- Confirmed `taras-and-lisa` is live as a Cloudflare Worker (not classic Pages) via `wrangler deployments list`/`wrangler pages project list`; found the `cloudflare/workers-autoconfig` branch has one real, substantive commit — left unmerged, owner to decide.
- `.strict()` added to all `site/src/content.config.ts` schemas, closing the gap noted in the previous entry.
- Fixed the deploy-failure Telegram message's `/undo` wording to clarify it can't revert manual GitHub edits.
- `observability: { enabled: true, head_sampling_rate: 1 }` set on `bot/wrangler.jsonc`. (`site/`'s equivalent config only exists on the un-merged branch above.)

All entries below are from the initial build-out on **2026-07-17** (multiple sessions same day).

- **README.md** rewritten as a portfolio-quality doc (architecture diagram, tech stack, roadmap) — corrected to describe Workers-with-static-assets + Workers Builds, not Pages/webhooks.
- **`/undo` scope investigated** (read-only session) and confirmed as documented above; identified `site/src/data/portfolio.json`'s `ctaEmail` field as a safe manual break/restore target for deploy-failure testing.
- **Deploy notifications, three iterations**, each superseding the last but left in place as dormant code: (1) a `POST /webhooks/pages-deploy` endpoint guarded by `CF_WEBHOOK_SECRET` — abandoned, needs a paid Cloudflare plan; (2) an `email()` handler parsing Cloudflare notification emails via `postal-mime` — abandoned once Workers Builds' structured events became available (also fixed a regex bug where "project" + any word wrongly matched unrelated prose); (3) **the current active path** — a `queue()` handler consuming Workers Builds events from the `deploy-events` Queue, later filtered to `branch === "main"` only. All three share message-composition helpers with consistent 🚀/❌/ℹ️ wording. Local testing gotcha discovered and reused throughout: this Wrangler version (4.111.0) has no local Queues/email simulation, so handlers were tested by direct `tsx` invocation of the exported function with a hand-built batch/message.
- **Structured logging** added via Workers Logs (`bot/src/logger.ts`, `logEvent`) across all handlers — message lifecycle, LLM calls/tool use, content validation, GitHub commits, `/undo`, uncaught errors. Hard rule: never log secrets. (Superseded 2026-07-23, see above.)
- **Agent tone pass** — added a TONE AND STYLE block to the system prompt (casual, user's language) without touching any behavioral rule.
- **`/undo` command added**, completing Stage 3. Confirmed live via real double-undo on Telegram (commit `4e1f60f` then `150594e`).
- **Incident:** owner asked the bot to add a field (`email`) that isn't in `site.json`'s schema. Zod's default mode silently stripped the unknown key, producing an empty-diff "successful" commit, and the bot later went fully unresponsive with no fallback reply anywhere. Three-part fix: `.strict()` on every content schema, an explicit system-prompt rule against inventing fields, and the outer try/catch + fallback Telegram reply now in `index.ts`.
- **Bootstrap:** diary created; `bot/`+`dev-diary.md` fixed from being untracked in git; `github.ts`, `content-schemas.ts`, `agent.ts` added and wired end-to-end into `bot.ts`'s message handler; KV-backed conversation memory and `/reset` added; secrets/vars wired into `Env`.
